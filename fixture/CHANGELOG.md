# Changelog

## 0.3.0

- CSV export (`--csv`).
- Per-tag breakdown (`--by-tag`).
- Started the parsing-layer rewrite; `shiftlog/parse.py` is not finished yet.

## 0.2.0

- Per-project summary with a total row.
- Read timesheets from a path instead of stdin.

## 0.1.0

- First cut.
