shared scratch - several branches dump here, do not clear

scratch space - parser rewrite

- old duration regex lived in parse.py before the rewrite:
    r'^(\d+)h(\d+)m$'
  it only ever handled the compound form
- keep tiny.txt around, the CLI smoke test reads it
